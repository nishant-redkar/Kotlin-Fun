<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewHomeApp" parent="Theme.Material3.DayNight.NoActionBar">





        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="NoRippleStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="rippleColor">@android:color/transparent</item>
    </style>

    <style name="BottomNavigationView.Item" parent="Widget.Design.BottomNavigationView">
        <!-- Remove or modify attributes related to active state appearance -->
        <item name="android:background">@null</item> <!-- Remove background -->
        <item name="android:textColor">#000000</item> <!-- Set text color to black -->
        <item name="android:tint">@null</item> <!-- Remove icon tint -->
    </style>


</resources>